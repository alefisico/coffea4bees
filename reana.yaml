version: 0.9.2
inputs:
  directories:
    - .ci-workflows
    - python
  parameters:
    workdir: '$REANA_WORKSPACE'
workflow:
  type: yadage
  file: .ci-workflows/reana_workflow.yml
outputs:
  files:
    - analysis/hists/histAll.coffea 

